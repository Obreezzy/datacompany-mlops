name: DataCompany ML Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  train-test-build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Cache pip packages
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}

      - name: Install dependencies
        run: pip install -r requirements.txt pytest

      - name: Train model and run quality gate
        run: python train.py

      - name: Run API tests
        run: pytest test_api.py -v

      - name: Run model monitoring
        run: python monitor.py

      - name: Show model metadata
        run: cat model/metadata.json

      - name: Build Docker image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/datacompany-risk-model:latest .

      # CD step - log in and push using direct docker commands
      - name: Log in to Docker Hub and push
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
          docker push ${{ secrets.DOCKER_USERNAME }}/datacompany-risk-model:latest
          echo "================================================"
          echo "Deployment complete"
          echo "Image: ${{ secrets.DOCKER_USERNAME }}/datacompany-risk-model:latest"
          echo "================================================"